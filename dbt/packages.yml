packages:
  # Core dbt utility macros (standard)
  - package: dbt-labs/dbt_utils
    version: [">=1.1.1", "<2.0.0"]

  # Date helpers (compatible with dbt-core 1.10.x)
  - package: godatadriven/dbt_date
    version: "0.15.0"

  # Data quality & expectations (like Great Expectations but dbt-native)
  - package: metaplane/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
