version: 2

seeds:
  - name: custom_tickers
    description: "A static list of company with the most viewed options chain."
    columns:
      - name: ticker
        description: "The stock ticker symbol."
        tests:
          - unique
          - not_null
